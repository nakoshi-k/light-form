import toForm , {bracketed,doted} from "./to-form";
import toObject from "./to-object";
import fileSupport from "./file-support";

export default {
    toForm,
    toObject,
    bracketed,
    doted,
    fileSupport
}