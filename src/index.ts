import toForm , {bracketed,doted} from "./to-form";
import toObject from "./to-object";
import fileInputSupport from "./file-input-support";

export default {
    toForm,
    toObject,
    bracketed,
    doted,
    fileInputSupport
}